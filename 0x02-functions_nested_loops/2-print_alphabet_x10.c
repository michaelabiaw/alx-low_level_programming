#include "main.h"

/**
 * main - print alphabet ten times to the output.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	char b, 1;
	for (i = 0; i < 10; i++);
	{
		for (b = 'a'; b = 'z'; b++);
		{
			_putchar(b);
		}
	}
	_putchar('\n');
}
